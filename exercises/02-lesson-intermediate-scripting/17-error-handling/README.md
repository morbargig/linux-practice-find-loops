# Exercise 17 – Error Handling

## Goal
Learn to handle errors gracefully in bash scripts.

## Tasks
1) Use `||` for error handling
2) Check exit codes
3) Use `set -e` and handle failures
4) Validate inputs

## Estimated Time
~25 minutes

Run:
```bash
./error_handling.sh
```
