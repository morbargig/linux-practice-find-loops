# Exercise 11 – File Operations

## Goal
Practice file operations: `cp`, `mv`, `mkdir`, and `rm`.

## Tasks
1) Create a new directory
2) Copy a file to the new directory
3) Rename a file using `mv`
4) Remove a temporary file

## Estimated Time
~25 minutes

Run:
```bash
./file_ops.sh
```
