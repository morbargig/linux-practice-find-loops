# Exercise 10 – Basic Commands

## Goal
Practice essential Linux commands: `ls`, `cat`, `wc`, and `echo`.

## Tasks
1) List files in `../../lab/files` with details (`ls -l`)
2) Display contents of a file (`cat`)
3) Count lines in a log file (`wc -l`)
4) Print formatted output with `echo`

## Estimated Time
~20 minutes

Run:
```bash
./basic_commands.sh
```
