# Exercise 01 – Tools

## Goal
Install and verify common Linux tools.

## Tasks
1) Check if `tree` is installed (`which tree`)
2) Install if missing (`sudo apt install tree`)
3) Run `tree` on `../../lab/files`
