# Exercise 06 – find + Loop

## Goal
Combine `find` with loops to process multiple files.

## Tasks
1) Find all `.log` files
2) Loop through them and print filename + line count

Run:
```bash
./process_logs.sh
```
