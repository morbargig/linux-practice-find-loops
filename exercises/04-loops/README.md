# Exercise 04 – Loops

## Goal
Practice `for` and `while` loops in bash.

## Tasks
1) Loop over `.txt` files and print name + size (`for_loop.sh`)
2) Read a log file line-by-line (`while_loop.sh`)

Run:
```bash
./for_loop.sh
./while_loop.sh
```
